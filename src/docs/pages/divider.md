# Divider

## Default

this is defualt Divider component

```js
Divider();
```

## Colors

```js
View("divider with primary color"),
Divider({ color: "primary" }),

View("divider with secondary color"),
Divider({ color: "secondary"}),

View("divider with success color"),
Divider({ color: "success" }),

View("divider with info color"),
Divider({ color: "info" }),

View("divider with warning color"),
Divider({ color: "warning" }),

View("divider with error color"),
Divider({ color: "error" }),

View("divider with light color"),
Divider({ color: "light" }),

View("divider with dark color"),
Divider({ color: "dark" }),
```